@model IEnumerable<ShadrackPhoneRepair.Models.DeviceProblem>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Template.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CostOfP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CostOfP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DeviceProblemId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DeviceProblemId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DeviceProblemId })
        </td>
    </tr>
}

</table>
