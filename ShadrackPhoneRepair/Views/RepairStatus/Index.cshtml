@model IEnumerable<ShadrackPhoneRepair.Models.RepairStatus>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Template.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RepairStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RepairStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RepairStatusId })
        </td>
    </tr>
}

</table>
